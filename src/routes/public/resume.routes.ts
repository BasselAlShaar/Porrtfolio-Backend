import { Router } from "express";
//import resumeController from "../../controllers/public/resume.controller.js";

const resumeRouterPublic = Router();

//Get Resume
//resumeRouterPublic.get('/', resumeController.getActiveResume);

export default resumeRouterPublic;